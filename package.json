{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm -r run dev",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "setup:all": "pnpm --filter user-service run migrate && pnpm --filter user-service run generate && pnpm --filter product-service run migrate && pnpm --filter product-service run generate && pnpm --filter order-service run migrate && pnpm --filter order-service run generate && pnpm dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@types/node": "^22.19.17",
    "@types/express": "^5.0.6",
    "tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "zod": "^4.4.3",
    "express": "^5.2.1"
  }
}
